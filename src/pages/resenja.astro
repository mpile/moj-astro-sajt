---
// src/pages/resenja.astro
import Layout from '../layouts/layout.astro';

const resenja = [
  {
    id: 'prevoz',
    naziv: 'SaaS za prevoz putnika',
    namena: 'Za firme koje se bave prevozom putnika',
    problemi: [
      'Praćenje voznog parka',
      'Rezervacije i raspored',
      'Fakturisanje putnika',
      'Izveštaji za knjigovodstvo'
    ],
    boja: '#1e3a5f', // tamno plava
    ikonica: '🚐',
    slug: '/resenja/prevoz'
  },
  {
    id: 'auto-skola',
    naziv: 'SaaS za auto-škole',
    namena: 'Za auto-škole u Crnoj Gori',
    problemi: [
      'Administracija kandidata (upis, dokumenta, evidencija)',
      'Planiranje i evidentiranje časova (nema online nastave)',
      'Praćenje prisustva i napretka polaznika',
      'Evidencija prijava za ispit (administrativna podrška)'
    ],
    boja: '#b76e3c', // topla nijansa
    ikonica: '🚗',
    slug: '/resenja/auto-skole'
  }
];
---

<Layout title="Rešenja za vaš biznis | Art-Info">
  
  <div class="resenja-header">
    <h1>Specijalizovana <span class="accent">rešenja</span></h1>
    <p class="subtitle">Softver koji rešava stvarne probleme vaše branše</p>
  </div>
  
  <!-- Dva rešenja - lepo uporedo -->
  <div class="resenja-grid">
    {resenja.map(resenje => (
      <div class="resenje-card" style={`border-top-color: ${resenje.boja}`}>
        <div class="card-header">
          <span class="ikonica">{resenje.ikonica}</span>
          <h2>{resenje.naziv}</h2>
        </div>
        
        <p class="namena">{resenje.namena}</p>
        
        <div class="problemi">
          <h3>Rešava:</h3>
          <ul>
            {resenje.problemi.map(problem => (
              <li>✓ {problem}</li>
            ))}
          </ul>
        </div>
        
        <div class="card-footer">
          <a href={resenje.slug} class="btn-saznaj" style={`background: ${resenje.boja}`}>
            Saznaj više →
          </a>
        </div>
      </div>
    ))}
  </div>
  
  <!-- Zajednička CTA -->
  <div class="cta-section">
    <h2>Zainteresovani ste?</h2>
    <p>Prikažemo vam kako softver radi i prilagodimo ga vašim potrebama.</p>
    <a href="/kontakt" class="btn-primary">Zakažite prezentaciju</a>
  </div>
  
</Layout>

<style>
  .resenja-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .resenja-header h1 {
    font-size: 2.5rem;
    color: var(--primary-dark);
    margin-bottom: 0.5rem;
  }
  
  .accent {
    color: var(--primary-medium);
    font-weight: 300;
  }
  
  .subtitle {
    color: var(--gray-600);
    font-size: 1.2rem;
  }
  
  .resenja-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin: 3rem 0;
  }
  
  .resenje-card {
    background: white;
    border-radius: 16px;
    padding: 2rem;
    box-shadow: var(--shadow-md);
    border-top: 6px solid transparent;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }
  
  .resenje-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
  }
  
  .card-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }
  
  .ikonica {
    font-size: 2.5rem;
  }
  
  .card-header h2 {
    color: var(--primary-dark);
    font-size: 1.5rem;
    margin: 0;
  }
  
  .namena {
    color: var(--gray-600);
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--gray-200);
  }
  
  .problemi h3 {
    color: var(--primary-dark);
    font-size: 1.1rem;
    margin-bottom: 1rem;
  }
  
  .problemi ul {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
  }
  
  .problemi li {
    color: var(--gray-700);
    padding: 0.5rem 0;
    border-bottom: 1px dashed var(--gray-200);
  }
  
  .problemi li:last-child {
    border-bottom: none;
  }
  
  .card-footer {
    margin-top: auto;
  }
  
  .btn-saznaj {
    display: inline-block;
    width: 100%;
    padding: 1rem;
    color: white;
    text-decoration: none;
    border-radius: 8px;
    text-align: center;
    font-weight: 500;
    transition: all 0.3s ease;
  }
  
  .btn-saznaj:hover {
    opacity: 0.9;
    transform: translateX(5px);
  }
  
  .cta-section {
    text-align: center;
    padding: 3rem;
    background: linear-gradient(135deg, var(--gray-100) 0%, white 100%);
    border-radius: 16px;
    margin: 3rem 0;
  }
  
  .cta-section h2 {
    color: var(--primary-dark);
    margin-bottom: 1rem;
  }
  
  .cta-section p {
    color: var(--gray-700);
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }
  
  .btn-primary {
    display: inline-block;
    padding: 1rem 3rem;
    background: var(--primary-dark);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s ease;
  }
  
  .btn-primary:hover {
    background: var(--primary-medium);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  @media (max-width: 768px) {
    .resenja-grid {
      grid-template-columns: 1fr;
    }
    
    .resenja-header h1 {
      font-size: 2rem;
    }
    
    .cta-section {
      padding: 2rem;
    }
  }
</style>